import { ComingSoon } from "@/components/ui/ComingSoon";

export const metadata = { title: "Öğren" };

export default function OgrenPage() {
  return (
    <ComingSoon
      title="Öğren"
      description="5 ünite, 27 ders — SELECT'ten pencere fonksiyonlarına kadar SQL'i sıfırdan öğren."
    />
  );
}
